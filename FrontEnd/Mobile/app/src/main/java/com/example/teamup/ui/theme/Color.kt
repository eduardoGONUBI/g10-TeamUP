// app/src/main/java/com/example/teamup/ui/theme/Color.kt
package com.example.teamup.ui.theme

import androidx.compose.ui.graphics.Color

// Cores principais
val Primary       = Color(0xFF00796B) // verde-teal
val OnPrimary     = Color(0xFFFFFFFF) // branco

// Cores secundárias / acentuação
val Secondary     = Color(0xFFF0ECD2) // creme claro
val OnSecondary   = Color(0xFF000000) // preto

val Tertiary      = Color(0xFFB2DFDB) // verde muito claro
val OnTertiary    = Color(0xFF000000) // preto

// **Fundo e superfícies**
val Background    = Color(0xFFFFFFFF) // branco puro
val OnBackground  = Color(0xFF000000) // preto

val Surface       = Color(0xFFFFFFFF) // branco puro
val OnSurface     = Color(0xFF000000) // preto
